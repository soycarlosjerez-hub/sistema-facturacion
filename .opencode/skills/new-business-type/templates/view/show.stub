@extends('layouts.app')

@section('title', '{{ENTITY_LABEL}}')

@section('content')
<div class="container-fluid py-4">
    <div class="row justify-content-center">
        <div class="col-lg-10">
            <!-- Header -->
            <div class="d-flex justify-content-between align-items-center mb-4">
                <div>
                    <h4 class="mb-1"><i class="bi bi-info-circle me-2"></i>{{ENTITY_LABEL}} Detalle</h4>
                    <nav aria-label="breadcrumb">
                        <ol class="breadcrumb mb-0">
                            <li class="breadcrumb-item"><a href="{{ route('dashboard') }}">Dashboard</a></li>
                            <li class="breadcrumb-item"><a href="{{ route('{{ROUTE_PREFIX}}.index') }}">{{ENTITY_LABEL_PLURAL}}</a></li>
                            <li class="breadcrumb-item active">{{SHOW_TITLE}}</li>
                        </ol>
                    </nav>
                </div>
                <div class="d-flex gap-2">
                    <a href="{{ route('{{ROUTE_PREFIX}}.edit', ${{SINGULAR_LOWER}}) }}" class="btn btn-warning rounded-pill">
                        <i class="bi bi-pencil me-1"></i> Editar
                    </a>
                    <a href="{{ route('{{ROUTE_PREFIX}}.index') }}" class="btn btn-outline-secondary rounded-pill">
                        <i class="bi bi-arrow-left me-1"></i> Volver
                    </a>
                </div>
            </div>

            {{-- Info Card --}}
            <div class="card shadow-sm border-0">
                <div class="card-body p-4">
                    <div class="row g-4">
{{DETAIL_FIELDS}}
                    </div>

                    {{-- Relationships Section --}}
                    @if(isset($relationships))
                        <hr class="my-4">
                        <h5 class="mb-3">Relaciones</h5>
                        <div class="row g-4">
{{RELATIONSHIP_SECTIONS}}
                        </div>
                    @endif
                </div>
            </div>
        </div>
    </div>
</div>
@endsection
